#!/bin/bash
# Create a test phrase with locked children using the REST API (assuming the backend takes care of it, or we can just test the local save endpoint)
echo "This test is better done manually or by adding a dedicated test endpoint since the Firebase Admin SDK requires a service account credential."
